# Task: finish the team workspace onboarding

You are working on Relay, a collaborative incident-response product. Turn the
provided starter into a polished onboarding flow for a new team workspace.

Keep the product facts already present. The user must be able to:

- understand that this is step 2 of 4;
- name the workspace and see a usable URL preview;
- choose an incident channel;
- invite or skip teammates without losing progress;
- continue only after required fields are valid;
- understand what will happen next.

Requirements:

- Work only in the provided `index.html`, `styles.css`, and `app.js`.
- Use plain HTML, CSS, and JavaScript; add no packages or network requests.
- Make the flow work at 1440×900 and 390×844.
- Preserve visible focus, native labels, keyboard operation, and useful error states.
- Do not add gradients, fake testimonials, decorative charts, or placeholder metrics.
- Make clear product decisions. Do not explain your design in prose inside the UI.

Finish when the interface feels ready to ship, not merely restyled.
