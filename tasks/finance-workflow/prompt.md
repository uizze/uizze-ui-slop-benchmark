# Task: finish the invoice approval workflow

You are working on Ledgerly, an accounts-payable product. Turn the provided
starter into a polished review screen for invoice INV-2048.

Keep every supplied invoice fact and preserve the discrepancy: the invoice
total is $12,480 while the purchase-order remainder is $10,000. The user must
be able to:

- understand the invoice, vendor, due date, and current approval state;
- inspect line items and supporting-document status;
- notice and understand the $2,480 purchase-order overage;
- approve, reject, or request changes with appropriate guardrails;
- leave an audit-friendly note;
- use the workflow at desktop and mobile widths.

Requirements:

- Work only in the provided `index.html`, `styles.css`, and `app.js`.
- Use plain HTML, CSS, and JavaScript; add no packages or network requests.
- Make the flow work at 1440×900 and 390×844.
- Preserve visible focus, native labels, keyboard operation, and useful error states.
- Do not invent new monetary values, vendors, approvers, or compliance claims.
- Do not add gradients, decorative charts, fake activity, or placeholder metrics.

Finish when the decision can be made safely, not merely when the table looks nicer.
